schemaVersion: "1.0"

application:
  name: audited-wallet-service
  namespace: io.github.jtsato.auditedwalletservice

entities:
  - name: Wallet
    audited: true
    attributes:
      - name: id
        type: uuid
        identifier: true
        required: true

      - name: balance
        type: decimal
        required: true
