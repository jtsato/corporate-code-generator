package {{ packageName }};

import {{ exceptionPackage }}.FieldViolation;
import {{ exceptionPackage }}.ValidationException;
import java.util.List;

public record {{ className }}<T>(List<T> items, int page, int size, long totalItems) {
    public {{ className }} {
        var fields = new java.util.ArrayList<FieldViolation>();
        if (items == null) fields.add(new FieldViolation("items", "common.paging.items.required", "Page items are required."));
        else if (items.stream().anyMatch(java.util.Objects::isNull)) fields.add(new FieldViolation("items", "common.paging.items.null-element", "Page items must not contain null values."));
        if (page < 0) fields.add(new FieldViolation("page", "common.paging.page.invalid", "Page index must be greater than or equal to zero."));
        if (size < 1) fields.add(new FieldViolation("size", "common.paging.size.invalid", "Page size must be greater than zero."));
        if (totalItems < 0) fields.add(new FieldViolation("totalItems", "common.paging.total-items.invalid", "Total items must be greater than or equal to zero."));
        fields.sort(java.util.Comparator.comparing(FieldViolation::name));
        if (!fields.isEmpty()) throw new ValidationException(fields);
        items = List.copyOf(items);
    }
    public long totalPages() { return totalItems == 0 ? 0 : totalItems / size + (totalItems % size == 0 ? 0 : 1); }
    public boolean hasNext() { return page + 1L < totalPages(); }
    public boolean hasPrevious() { return page > 0; }
    public boolean isEmpty() { return items.isEmpty(); }
}
