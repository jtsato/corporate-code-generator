services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: {{ applicationName }}:latest
    ports:
      - "{{ containerPort }}:{{ containerPort }}"
    restart: unless-stopped
