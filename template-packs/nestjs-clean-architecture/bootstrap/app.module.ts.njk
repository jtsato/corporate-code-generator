import { Module } from '@nestjs/common';

{% for entity in entities -%}
import { {{ entity.className }}Module } from './web-api/entrypoints/{{ entity.pluralFileName }}/{{ entity.fileName }}.module';
{% endfor %}
@Module({
  imports: [
{%- for entity in entities %}
    {{ entity.className }}Module,
{%- endfor %}
  ],
})
export class AppModule {}
